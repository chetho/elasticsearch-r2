name: Build and Push Docker Image

on:
  push:
    branches: [ develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  security-events: write
  actions: read

env:
  DOCKER_REGISTRY: docker.io
  IMAGE_NAME: elasticsearch-r2

jobs:
  security-scan:
    name: Security Scan & Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Hadolint Dockerfile linter
      uses: hadolint/hadolint-action@v3.1.0
      with:
        dockerfile: Dockerfile
        format: sarif
        output-file: hadolint-results.sarif

    - name: Upload Hadolint scan results
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      continue-on-error: true
      with:
        sarif_file: 'hadolint-results.sarif'

    - name: Show Hadolint results (if SARIF upload failed)
      if: always()
      run: |
        echo "📋 Hadolint Results:"
        cat hadolint-results.sarif | jq -r '.runs[0].results[]? | "❌ \(.ruleId): \(.message.text)"' || echo "✅ No Hadolint issues found"

    - name: Check base image is official Elasticsearch
      run: |
        if ! grep -q "FROM docker.elastic.co/elasticsearch/elasticsearch:" Dockerfile; then
          echo "Warning: Not using official Elasticsearch image from docker.elastic.co"
          echo "Please use: FROM docker.elastic.co/elasticsearch/elasticsearch:VERSION"
          exit 1
        fi
        echo "✅ Using official Elasticsearch image"

  build:
    name: Build & Security Test
    runs-on: ubuntu-latest
    needs: security-scan
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        platforms: linux/arm64
    
    - name: Log in to Docker Hub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
    
    - name: Extract Elasticsearch version from Dockerfile
      id: es_version
      run: |
        ES_VERSION=$(grep "FROM docker.elastic.co/elasticsearch/elasticsearch:" Dockerfile | sed 's/.*elasticsearch://')
        echo "elasticsearch_version=$ES_VERSION" >> $GITHUB_OUTPUT
        echo "📦 Detected Elasticsearch version: $ES_VERSION"

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=raw,value=${{ steps.es_version.outputs.elasticsearch_version }},enable=true
          type=raw,value=${{ steps.es_version.outputs.elasticsearch_version }}-{{branch}},enable={{is_default_branch}}
          type=raw,value=latest,enable={{is_default_branch}}
        labels: |
          org.opencontainers.image.title=${{ env.IMAGE_NAME }}
          org.opencontainers.image.description=Elasticsearch with R2 plugin for ARM64/AMD64
          org.opencontainers.image.version=${{ steps.es_version.outputs.elasticsearch_version }}
          org.opencontainers.image.vendor=Community
          elasticsearch.version=${{ steps.es_version.outputs.elasticsearch_version }}
    
    - name: Build for scanning (ARM64 primary)
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/arm64
        load: true
        tags: ${{ env.IMAGE_NAME }}:scan
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Security scanning of built image
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.IMAGE_NAME }}:scan
        format: 'sarif'
        output: 'trivy-results.sarif'
        severity: 'CRITICAL,HIGH'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      continue-on-error: true
      with:
        sarif_file: 'trivy-results.sarif'

    - name: Run Trivy vulnerability scanner (Table format)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.IMAGE_NAME }}:scan
        format: 'table'
        severity: 'CRITICAL,HIGH'

    - name: Run Trivy vulnerability scanner (JSON report)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.IMAGE_NAME }}:scan
        format: 'json'
        output: 'trivy-report.json'

    - name: Security Summary
      if: always()
      run: |
        echo "🔍 Security Scan Summary for ARM64 Elasticsearch:"
        echo "================================================"
        
        # Count vulnerabilities by severity
        CRITICAL=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-report.json 2>/dev/null || echo "0")
        HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-report.json 2>/dev/null || echo "0")
        MEDIUM=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length' trivy-report.json 2>/dev/null || echo "0")
        LOW=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="LOW")] | length' trivy-report.json 2>/dev/null || echo "0")
        
        echo "🔴 CRITICAL: $CRITICAL"
        echo "🟠 HIGH: $HIGH"
        echo "🟡 MEDIUM: $MEDIUM"
        echo "🟢 LOW: $LOW"
        echo ""
        
        # Debug: Show all vulnerabilities to understand the discrepancy
        echo "🔍 Debug: All vulnerabilities found (first 10):"
        jq -r '.Results[]?.Vulnerabilities[]? | "\(.Severity): \(.VulnerabilityID) - \(.Title // .Description) (Package: \(.PkgName))"' trivy-report.json 2>/dev/null | sort | head -10
        echo ""
        
        # Show HIGH vulnerabilities if any
        if [ "$HIGH" -gt 0 ]; then
          echo "⚠️  HIGH Severity Vulnerabilities:"
          jq -r '.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH") | "- \(.VulnerabilityID): \(.Title // .Description) (Package: \(.PkgName))"' trivy-report.json 2>/dev/null
          echo ""
        fi
        
        # Show top HIGH vulnerabilities
        if [ "$HIGH" -gt 0 ]; then
          echo "⚠️  Top HIGH Severity Vulnerabilities:"
          jq -r '.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH") | "- \(.VulnerabilityID): \(.Title // .Description) (Package: \(.PkgName))"' trivy-report.json 2>/dev/null | head -5
          echo ""
        fi
        
        # Security gate - strict for PRs, informational for pushes
        if [ "$CRITICAL" -gt 0 ]; then
          echo "❌ SECURITY ALERT: $CRITICAL CRITICAL vulnerabilities found!"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "� BLOCKING PR: Critical vulnerabilities must be fixed before merge"
            exit 1
          else
            echo "�🚨 Strongly recommend fixing before production use."
          fi
        fi
        
        if [ "$HIGH" -gt 0 ]; then
          echo "⚠️  SECURITY WARNING: $HIGH HIGH vulnerabilities found"
          echo "� Consider updating base image or fixing vulnerabilities when possible."
          echo "� Allowing PR to proceed - HIGH vulnerabilities are warnings only"
        fi
        
        if [ "${{ github.event_name }}" = "pull_request" ]; then
          if [ "$CRITICAL" -eq 0 ]; then
            echo "✅ PR SECURITY GATE PASSED: No CRITICAL vulnerabilities found"
            echo "🔒 Safe to merge to main branch"
            if [ "$HIGH" -gt 0 ]; then
              echo "ℹ️  Note: $HIGH HIGH vulnerabilities present but not blocking"
            fi
          fi
        else
          echo "✅ Build will continue - Security scanning is informational for pushes"
        fi
        
        echo "📊 Download the trivy-security-report artifact for detailed analysis"

    - name: Upload Trivy report as artifact
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: trivy-security-report
        path: trivy-report.json

    - name: Docker Scout CVE scan
      uses: docker/scout-action@v1
      if: github.event_name != 'pull_request'
      with:
        command: cves
        image: ${{ env.IMAGE_NAME }}:scan
        sarif-file: scout-report.sarif
        summary: true

    - name: Upload Docker Scout scan results
      uses: github/codeql-action/upload-sarif@v3
      if: always() && github.event_name != 'pull_request'
      continue-on-error: true
      with:
        sarif_file: 'scout-report.sarif'

    - name: Build and push (ARM64 primary)
      uses: docker/build-push-action@v5
      if: github.event_name != 'pull_request'
      with:
        context: .
        platforms: linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha

    - name: Update Docker Hub description
      if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
      uses: peter-evans/dockerhub-description@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
        repository: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
        readme-filepath: ./README.md

  cleanup:
    name: Cleanup Docker Hub
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
    
    - name: Delete develop branch images
      run: |
        echo "🧹 Cleaning up develop branch images from Docker Hub..."
        
        # Get repository name
        REPO="${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}"
        
        # Function to get auth token for Docker Hub API
        get_token() {
          curl -s -H "Content-Type: application/json" \
            -X POST \
            -d "{\"username\": \"${{ secrets.DOCKER_USERNAME }}\", \"password\": \"${{ secrets.DOCKER_ACCESS_TOKEN }}\"}" \
            https://hub.docker.com/v2/users/login/ | jq -r .token
        }
        
        # Get authentication token
        TOKEN=$(get_token)
        
        if [ "$TOKEN" = "null" ] || [ -z "$TOKEN" ]; then
          echo "❌ Failed to authenticate with Docker Hub API"
          exit 1
        fi
        
        echo "✅ Successfully authenticated with Docker Hub"
        
        # Get all tags for the repository
        echo "📋 Fetching all tags for repository: $REPO"
        TAGS_RESPONSE=$(curl -s -H "Authorization: JWT $TOKEN" \
          "https://hub.docker.com/v2/repositories/$REPO/tags/?page_size=100")
        
        # Extract develop-related tags (tags that start with 'develop')
        DEVELOP_TAGS=$(echo "$TAGS_RESPONSE" | jq -r '.results[]? | select(.name | startswith("develop")) | .name')
        
        if [ -z "$DEVELOP_TAGS" ]; then
          echo "✅ No develop branch images found to delete"
          exit 0
        fi
        
        echo "🗑️  Found develop branch images to delete:"
        echo "$DEVELOP_TAGS"
        echo ""
        
        # Delete each develop tag
        DELETED_COUNT=0
        FAILED_COUNT=0
        
        for tag in $DEVELOP_TAGS; do
          echo "🗑️  Deleting tag: $tag"
          
          DELETE_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: JWT $TOKEN" \
            -X DELETE \
            "https://hub.docker.com/v2/repositories/$REPO/tags/$tag/")
          
          if [ "$DELETE_RESPONSE" = "204" ]; then
            echo "✅ Successfully deleted: $tag"
            ((DELETED_COUNT++))
          else
            echo "❌ Failed to delete: $tag (HTTP $DELETE_RESPONSE)"
            ((FAILED_COUNT++))
          fi
        done
        
        echo ""
        echo "🧹 Cleanup Summary:"
        echo "==================="
        echo "✅ Successfully deleted: $DELETED_COUNT images"
        echo "❌ Failed to delete: $FAILED_COUNT images"
        
        if [ $FAILED_COUNT -gt 0 ]; then
          echo "⚠️  Some deletions failed - check Docker Hub permissions"
          exit 1
        fi
        
        echo "🎉 All develop branch images successfully cleaned up!"

    - name: Cleanup summary
      if: always()
      run: |
        echo "📊 Docker Hub Cleanup Complete"
        echo "==============================="
        echo "Repository: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}"
        echo "Trigger: Push to main branch (typically after PR merge)"
        echo "Action: Removed all 'develop' prefixed tags to keep registry clean"
        echo ""
        echo "💡 Remaining images should only be:"
        echo "   - latest (main branch)"
        echo "   - version tags (v1.0.0, etc.)"
        echo "   - main branch specific tags"